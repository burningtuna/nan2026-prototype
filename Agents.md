# DIRECTIVE//12 개발 지침

## 프로젝트 목적

NAN 2026 NHN Game × AI Hackathon 사전 과제에 개인 자격으로 제출할 Godot 웹 게임 프로토타입을 제작한다.

- 제출 마감: 2026년 8월 10일
- 엔진: Godot 4.7.1
- 렌더러: GL Compatibility
- 플랫폼: 데스크톱 웹 브라우저, GitHub Pages
- 개발 형태: 개인 참가, AI 개발 도구 적극 활용
- 게임 내 언어: 영어
- 문서 언어: 한국어
- 가제: `DIRECTIVE//12`
- 한 줄 소개: `Assemble a mech. Program its instincts. Survive the arena.`

심사자가 별도 설치나 계정 없이 링크를 열어 즉시 플레이할 수 있어야 한다. 외부 API, 서버, API 키 또는 유료 서비스에 의존하지 않는다.

## 핵심 콘셉트

소형 코드 생성 픽셀 파츠로 메카를 조립하고, 조건 카드를 우선순위대로 배치해 전투 AI를 설계하는 탑뷰 2D 오토 배틀 게임이다. 상세 전투 규칙과 파츠 데이터는 `Gamesystem.md`를 기준으로 한다.

핵심 플레이 흐름:

1. 머리, 몸통, 양팔 장비, 백팩, 다리를 조립한다.
2. 머리와 몸통 슬롯에 AI·인지·발전·방열 칩을 장착한다.
3. AI 조건 카드를 선택하고 우선순위를 정한다.
4. 자동 전투를 관전한다.
5. 파츠 피해, 명중률, 과열 시간, 카드 발동 기록을 확인한다.
6. 빌드를 수정해 다음 전투에 도전한다.
7. 다섯 스테이지를 거쳐 아레나에서 외계 세력과의 최종전까지 진행한다.
8. 캠페인 결과에 따라 엔딩과 스커미시 해금을 확인한다.

개별 전투는 1~5분, 브리핑과 격납고를 포함한 전체 플레이는 30분 이내를 목표로 한다. 상세 서사는 `Storyline.md`, 구현 순서는 `Developmentplan.md`를 기준으로 한다.

## 프로토타입 범위

반드시 포함할 기능:

- 타이틀, 조립, 전투, 분석, 최종 결과 화면
- 머리, 몸통, 우완 장비, 좌완 장비, 백팩, 다리의 6개 파츠 조립
- 양팔에 실탄·미사일·에너지 무기 또는 방패 장착
- 머리와 몸통의 모듈 슬롯 및 AI·인지·발전·방열 칩
- AI 조건 카드 선택 및 순서 변경
- 다섯 스테이지와 스테이지별 적 빌드
- 아레나, 도시, UFO 전장을 재사용 가능한 전투 시스템으로 구성
- 파츠별 내구도, 중량, 전력, 방열, 기동 및 기능 상실
- 파츠 크기와 실제 공격 경로를 반영한 부위 조준
- AI 칩 탑재 파츠 파괴 시 전투불능
- 전면만 방어하는 방향성 방패와 이를 우회하는 공격
- 접근, 후퇴, 측면 이동, 조준, 사격, 냉각 행동
- 고정 시드를 사용하는 재현 가능한 전투
- 현재 발동 중인 AI 카드와 판단 근거 표시
- 전투 후 주요 통계와 행동 기록 표시
- 스테이지 4의 구조 선택과 숨겨진 인간성 판정
- 스테이지 5의 EN·실탄·미사일 지원 편대와 UFO 레일건
- 생존한 지원 기체에 따라 달라지는 최종 보스전 구성
- 마우스만으로 완료할 수 있는 조작
- 웹 내보내기 및 GitHub Pages 배포

마감 전에는 다음 기능을 추가하지 않는다.

- 멀티플레이
- 서버 또는 계정 시스템
- 런타임 생성형 AI API 호출
- 자연어 명령 입력
- 복잡한 물리 조립
- 정의된 다섯 스테이지를 넘어서는 추가 캠페인
- 모바일 전용 조작
- 저장 시스템

## AI 조건 카드

기본 전투 AI는 이동, 몸통 조준, 사격, 냉각을 수행한다. 조건 카드는 기본 판단을 우선순위에 따라 덮어쓴다. 카드를 잘못 구성해도 기체가 완전히 정지하지 않도록 기본 행동은 항상 유지한다.

- AI 칩은 카드 수, 조건 복잡도, 판단 주기를 결정한다.
- 인지 칩은 카드가 사용할 수 있는 적 정보, 조준 정확도, 감지 주기와 동시 추적 수를 결정한다.
- 감지와 AI 판단 주기는 각각 `0.25`, `0.5`, `1`, `2`초 단계로 운용한다.
- 전력 부족과 과열은 감지 및 AI 판단 주기를 더 느린 단계로 낮춘다.
- 표적 카드는 머리·몸통·팔·백팩·다리 중 우선 공격할 파츠를 정한다.
- 방어 카드는 방패로 보호할 파츠나 방향을 정한다.
- 필요한 인지 정보가 없으면 해당 카드는 평가하지 않는다.
- 적의 내부 칩 위치는 처음에는 숨기고, 고급 인지를 통해 탐지한다.

초기 카드 목록:

| 카드 | 조건 및 효과 |
| --- | --- |
| `KEEP DISTANCE` | 적이 너무 가까우면 후퇴한다. |
| `CLOSE GAP` | 적이 사거리 밖이면 빠르게 접근한다. |
| `EMERGENCY VENT` | 과열 직전에 사격을 중단하고 냉각한다. |
| `TARGET CORE` | 적 AI 칩 위치를 탐지했다면 해당 파츠를 조준한다. |
| `DISARM THREAT` | 위협도가 가장 높은 무기를 장착한 팔을 조준한다. |
| `GUARD CORE` | 방패로 자신의 AI 칩 탑재 파츠를 방어한다. |

전투 화면에는 판단 과정을 짧게 노출한다.

```text
ACTIVE: EMERGENCY VENT
HEAT 86% >= THRESHOLD 80%
ACTION: RETREAT + COOL
```

카드 순서가 실제 행동과 승패에 영향을 주어야 하며, 결과 화면에서 그 차이를 설명할 수 있어야 한다.

## 기술 방향

- 내부 해상도는 480×270을 기준으로 한다.
- 픽셀 그래픽은 정수 좌표와 최근접 필터를 사용한다.
- 기체는 머리, 몸통, 양팔 장비, 백팩, 다리를 독립적으로 그려 조합 결과가 보이게 한다.
- 머리·몸통·다리는 12×12px, 긴 무장과 실루엣이 필요한 양팔 장비·백팩은 긴 변 24px와 짧은 변 12px 이내를 원칙으로 한다.
- 기체는 보행하지 않고 지상 부스트로 미끄러지듯 이동하며, 다리 파츠 자체에는 보행 애니메이션을 만들지 않는다.
- 각 다리 파츠는 로컬 부스트 앵커를 제공하고, 이동 애니메이션은 앵커에 재사용 가능한 점화·잔광 스프라이트를 붙여 표현한다.
- 외부 이미지 에셋 대신 GDScript로 픽셀 패턴과 제한된 팔레트를 생성한다.
- 전장, 투사체, 피격 및 폭발 효과도 가능한 한 코드로 생성한다.
- 게임 UI는 영어로 작성해 별도 한글 폰트 의존성을 피한다.
- 게임 데이터는 코드와 분리하고 JSON 또는 Godot Resource로 관리한다.
- 전투 로직과 화면 표현을 가능한 한 분리해 밸런스 시뮬레이션에 재사용한다.
- 같은 빌드와 같은 시드는 같은 결과를 내도록 무작위성을 통제한다.
- 웹 환경에서 안정적으로 동작하도록 스레드와 외부 네트워크 의존성을 사용하지 않는다.

목표 디렉터리 구조:

```text
nan2026-prototype/
├── Agents.md
├── Developmentplan.md
├── Gamesystem.md
├── Storyline.md
├── project.godot
├── scenes/
│   ├── main.tscn
│   ├── assembly_screen.tscn
│   ├── battle_screen.tscn
│   └── result_screen.tscn
├── scripts/
│   ├── game_controller.gd
│   ├── battle/
│   ├── ai/
│   ├── ui/
│   └── procedural_art/
├── data/
│   ├── parts.json
│   ├── directives.json
│   └── opponents.json
├── tools/
│   └── balance_simulator.gd
├── submission/
│   ├── game_description.md
│   ├── ai_technical_report.md
│   ├── prompts.md
│   └── licenses.md
└── docs/
    └── Web export
```

구조는 구현 과정에서 단순화할 수 있다. 작은 프로토타입에 불필요한 추상 계층이나 범용 프레임워크를 추가하지 않는다.

## AI 활용 및 증빙

AI 사용 자체가 제출물의 핵심이므로, 결과뿐 아니라 생성과 검토 과정을 재현할 수 있게 기록한다.

반드시 기록할 항목:

- 사용한 AI 도구와 모델
- 주요 프롬프트와 지시 사항
- AI가 제안하거나 생성한 기획, 코드, 데이터, 아트 규칙
- 사람이 검토하고 수정한 내용
- 채택하지 않은 제안과 사유
- 파츠 및 적 빌드 생성 과정
- 자동 전투 시뮬레이션 결과와 밸런스 조정 과정
- 외부 도구, 오픈소스 및 에셋의 출처와 라이선스

권장 작업 흐름:

```text
AI가 파츠와 적 빌드 후보 생성
→ 구조화 데이터 검증
→ 자동 전투 시뮬레이션
→ 승률, 피해량, 전투 시간 집계
→ AI가 이상 조합 분석
→ 사람이 최종 수치 결정
```

AI가 생성한 결과를 그대로 사용했다고 과장하지 않는다. `AI 제안 → 구현 → 테스트 → 인간 검토`의 반복 과정을 사실대로 기술한다.

## 제출물

개인 참가이므로 팀원 역할 기술서는 생략한다. 다음 네 가지를 완성해야 한다.

1. GitHub 저장소의 전체 소스와 GitHub Pages 웹 빌드
2. 실제 플레이 화면으로 구성한 30~60초 YouTube 영상
3. 게임 소개 및 실행 방법 PDF
4. AI 활용 기술 PDF

저장소에는 의미 있는 커밋 기록을 유지한다. 제출 링크는 심사 종료 시점까지 접근 가능해야 하며, 접수 마감 후 내용을 변경할 수 없다는 전제로 준비한다.

## 일정

상세 작업 순서와 단계별 완료 조건은 `Developmentplan.md`를 따른다.

| 날짜 | 목표 |
| --- | --- |
| 7월 31일 | 프로젝트 문서와 데이터 구조 확정 |
| 8월 1일 | 수동 테스트 기체, 실탄, 파츠 피격 |
| 8월 2일 | EN·열, 에너지·미사일·방패 |
| 8월 3일 | 감지 센서, AI 판단, 자동 전투 |
| 8월 4일 | 개발용 스커미시와 전투 UI |
| 8월 5일 | 격납고, 통신, 스테이지 실행기 및 전투 기능 동결 |
| 8월 6일 | 스테이지 1~3 |
| 8월 7일 | 스테이지 4 구조 분기와 스테이지 5 지원·레일건 |
| 8월 8일 | 압축형 최종전, 웹 배포와 브라우저 테스트 |
| 8월 9일 | 영상 촬영 및 PDF 작성 |
| 8월 10일 | 링크와 제출물 최종 검증 |

8월 5일 이후에는 새로운 전투 기능을 추가하지 않는다. 이후에는 기존 기능으로 스테이지 데이터와 연출을 구성하고 버그 수정, 가독성 개선, 제출물 검증만 수행한다.

## 완료 기준

- 새 브라우저 세션에서 링크를 열어 오류 없이 시작된다.
- 별도 설명 없이 마우스만으로 첫 전투를 시작할 수 있다.
- 개별 전투는 5분 이내, 전체 캠페인은 30분 이내에 완료할 수 있다.
- 다섯 스테이지와 엔딩을 중단 없이 완료할 수 있다.
- 파츠 조합이 외형과 전투 수치에 모두 반영된다.
- AI 카드 선택과 순서가 행동 및 결과에 관찰 가능한 차이를 만든다.
- 현재 AI 판단과 전투 후 분석을 통해 패배 원인을 이해할 수 있다.
- 외부 네트워크 요청 없이 전체 게임이 실행된다.
- Chromium 및 Firefox 계열 브라우저에서 웹 빌드를 확인한다.
- 모든 외부 소스와 라이선스가 문서에 기록된다.
- GitHub, YouTube, PDF의 링크와 접근 권한을 최종 확인한다.
