# SUBJECT//12 개발 지침

## 프로젝트 목적

NAN 2026 NHN Game × AI Hackathon 사전 과제에 개인 자격으로 제출할 Godot 웹 게임 프로토타입을 제작한다.

- 제출 마감: 2026년 8월 10일
- 엔진: Godot 4.7.1
- 렌더러: GL Compatibility
- 플랫폼: 데스크톱 웹 브라우저, GitHub Pages
- 개발 형태: 개인 참가, AI 개발 도구 적극 활용
- 게임 내 언어: 영어
- 문서 언어: 한국어
- 가제: `SUBJECT//12`
- 한 줄 소개: `Assemble a mech. Lead your wingman. Survive the arena.`

심사자가 별도 설치나 계정 없이 링크를 열어 즉시 플레이할 수 있어야 한다. 외부 API, 서버, API 키 또는 유료 서비스에 의존하지 않는다.

## 핵심 콘셉트

소형 코드 생성 픽셀 파츠로 메카를 조립해 직접 조작하고, 동일한 세팅을 복제한 AI 아군과 함께 싸우는 탑뷰 2D 메카 액션 게임이다. 플레이어는 자신을 인간 조종사라고 믿지만 실제로는 인간 행동을 모사하도록 학습된 전투 AI다. 상세 전투 규칙과 파츠 데이터는 `Gamesystem.md`를 기준으로 한다.

핵심 플레이 흐름:

1. 머리, 몸통, 양팔 장비, 백팩, 다리를 조립한다.
2. 플레이어와 같은 파츠·무장을 사용할 AI 아군의 전투 성향을 선택한다.
3. `WASD`와 마우스로 플레이어 기체를 직접 이동·조준·사격한다.
4. AI 아군과 협력해 아레나 팀전과 이후의 전쟁 임무를 수행한다.
5. 파츠 피해, 명중률, 피격 방향과 무기 운용 기록을 확인한다.
6. 빌드와 아군 성향을 수정해 다음 전투에 도전한다.
7. 다섯 스테이지를 거쳐 아레나에서 외계 세력과의 최종전까지 진행한다.
8. 캠페인 결과에 따라 엔딩과 이후 전투 기록의 의미 변화를 확인한다.

개별 전투는 1~5분, 브리핑과 격납고를 포함한 전체 플레이는 30분 이내를 목표로 한다. 상세 서사는 `Storyline.md`, 구현 순서는 `Developmentplan.md`를 기준으로 한다.

## 메인 화면과 게임 모드

메인 화면에는 처음부터 선택할 수 있는 네 개 메뉴가 있다. 게임 내 메뉴명은 영어로 표시한다.

- `STORY MODE`: 기존 다섯 스테이지 시나리오를 진행한다. 완료 스테이지와 시나리오 진행도를 로컬에 저장한다.
- `SKIRMISH MODE`: 격납고에서 확정한 기체와 같은 로드아웃의 AI 아군으로 적 메카 2기와 2대2 팀전을 진행한다. 현재 `combat_hud_test`가 이 모드의 기준 구현이다.
- `ENDLESS MODE`: 격납고에서 확정한 기체로 단독 출격해 무한히 보충되는 드론과 주기적으로 증원되는 메카를 상대하고 격파 점수를 쌓는다. 최고 점수를 로컬에 저장한다.
- `DELETE SAVE`: 스토리 진행도와 엔드리스 최고 점수를 항목별로 삭제한다. 삭제 전 확인을 요구하며 저장된 격납고 로드아웃은 유지한다.

스토리, 스커미시와 엔드리스는 캠페인 해금 조건 없이 처음부터 이용할 수 있다. 스토리 실행기가 완성되기 전 `STORY MODE`는 `STORY MODE // IN DEVELOPMENT` 안내를 표시한다.
스커미시와 엔드리스가 공유하는 격납고에는 로드아웃을 변경하지 않고 메인 화면으로 돌아가는 `MAIN MENU` 버튼을 제공한다.

## 프로토타입 범위

반드시 포함할 기능:

- 타이틀, 조립, 전투, 분석, 최종 결과 화면
- 머리, 몸통, 우완 장비, 좌완 장비, 백팩, 다리의 6개 파츠 조립
- 양팔에 실탄·미사일·에너지 무기 또는 방패 장착
- 데스크톱의 `WASD` 이동, 마우스 조준, 좌우 무기 발사와 부스트 조작
- 플레이어 로드아웃을 그대로 복제하는 AI 아군과 아군 전투 성향 선택
- 스테이지 1·2의 2대2 아레나 팀전과 스테이지 3의 2대1 보스전
- 다섯 스테이지와 스테이지별 적 빌드
- 아레나, 도시, UFO 전장을 재사용 가능한 전투 시스템으로 구성
- 파츠별 내구도, 중량, 전력, 방열, 기동 및 기능 상실
- 파츠 크기와 실제 공격 경로를 반영한 부위 조준
- 플레이어·아군·적이 공유하는 파츠별 피격과 전투불능 규칙
- 실제 잔탄 수만큼 아이콘을 표시하고 발사·재장전을 애니메이션으로 전달하는 4개 무장 HUD
- 격납고와 전투 HUD에서 공유하는 파츠 조립형 정면 와이어프레임
- 전면만 방어하는 방향성 방패와 이를 우회하는 공격
- 접근, 후퇴, 측면 이동, 조준, 사격, 냉각 행동
- 고정 시드를 사용하는 재현 가능한 전투
- 아군과 적의 현재 행동 상태, 준비 공격과 재장전 상태 표시
- 전투 후 주요 통계와 행동 기록 표시
- 스테이지 4의 구조 선택과 숨겨진 인간성 판정
- 스테이지 5의 EN·실탄·미사일 지원 편대와 UFO 레일건
- 생존한 지원 기체에 따라 달라지는 최종 보스전 구성
- 웹 내보내기 및 GitHub Pages 배포

후속 구현 필수:

- 5스테이지 종료 컷씬의 군 지휘관 대사 직후 이어지는 UFO 내부 최종전을 `res://scenes/final_battle.tscn`으로 구현해야 한다.
- 현재 `stage_05_cutscene.gd`는 최종전 씬이 없을 때 실행 오류를 막기 위해 스테이지 선택 화면으로 임시 복귀한다. 최종전 구현과 함께 이 폴백이 사용되지 않는 통합 흐름을 검증해야 한다.

마감 전에는 다음 기능을 추가하지 않는다.

- 멀티플레이
- 서버 또는 계정 시스템
- 런타임 생성형 AI API 호출
- 자연어 명령 입력
- 복잡한 물리 조립
- 정의된 다섯 스테이지를 넘어서는 추가 캠페인
- 모바일 전용 조작
- 다중 세이브 슬롯, 클라우드 저장 또는 계정 연동

## 플레이어 조작과 아군 AI

플레이어 기체는 자동전투가 아니라 직접 조작한다. 플레이어용 AI 카드·AI 칩 편성·조건 우선순위 시스템은 구현 범위에서 제외하고, 현재까지 만든 자동 이동·거리 유지·준비 감지 회피·재장전 후퇴 패턴은 아군과 적의 행동 프로필로 재사용한다.

데스크톱 기본 조작:

- `WASD`: 이동
- 마우스: 몸통과 무기 조준
- 마우스 좌우 버튼: 좌완·우완 무기 발사
- `Space`: 부스트 또는 대시
- 플레이어 사격은 수동이며 무기별 사거리, 발사각, 준비 시간, 탄창과 재장전 규칙을 그대로 적용한다.

입력 로직은 장치와 분리된 이동 벡터, 조준 벡터, 좌우 발사와 부스트 의도로 전달한다. 데스크톱 버전 완성 후 터치 입력을 지원한다면 왼쪽 가상 스틱은 이동, 오른쪽 가상 스틱은 조준을 담당하고 사격은 유효 사거리·발사각 안의 표적에 자동으로 수행한다. 모바일 입력은 데스크톱 버전 완성 전에는 구현하지 않는다.

아군 AI 규칙:

- 아군은 출격 시 플레이어의 현재 `MechLoadout`을 복제해 같은 파츠, 무장과 전투 수치를 사용한다.
- 격납고에서는 아군 파츠를 별도로 조립하지 않고 `AGGRESSIVE`, `DEFENSIVE`, `BALANCED` 중 전투 성향만 선택한다.
- `AGGRESSIVE`는 접근과 공격 기회를, `DEFENSIVE`는 플레이어 주변 유지와 위협 대응을, `BALANCED`는 거리 유지와 공격·회피 균형을 우선한다.
- 아군 AI라는 사실은 처음부터 플레이어에게 공개하며, 플레이어와 구분되는 색상·호출명·HUD 표식을 사용한다.
- 친선 피해는 사용하지 않는다. 플레이어가 파괴되면 패배하고, 아군이 파괴되어도 플레이어가 생존하면 전투는 계속된다.
- 스테이지 1과 2는 플레이어·아군 대 적 2기의 2대2 전투, 스테이지 3은 플레이어·아군 대 초고기동 보스의 2대1 전투로 구성한다.
- 아군 AI는 스테이지 3 정체 공개 이후 회수되며 스테이지 4와 5에는 등장하지 않는다.
- 플레이어 기체가 격파되고 같은 팀 아군이 생존해 있으면 전투 카메라는 생존 아군을 새 기준 유닛으로 추적한다. 해당 아군이 팀의 유일한 생존자가 되면 기존 성향과 관계없이 즉시 `AGGRESSIVE`로 전환한다.
- 전투 씬은 기존 다중 유닛 동적 프레이밍과 고정 줌 중심 카메라를 선택할 수 있다. 기본 HUD 테스트는 중심 카메라를 사용하며 평상시 플레이어, 플레이어 격파 후에는 생존 아군을 화면 정중앙에 둔다.
- 플레이어는 `Tab`으로 현재 센서 스냅샷에 포함된 적을 순환 선택하고 `Shift`로 플레이어 중심과 선택 표적 중심 카메라를 전환한다. 표적이 감지 범위를 벗어나거나 격파되면 표적 중심 모드를 해제한다.
- 중심 카메라는 마우스 휠로 줌인·줌아웃하며 플레이어와 표적 카메라가 같은 사용자 줌을 유지한다. 최대 줌아웃은 현재 뷰포트에 전장 `arena` 전체가 들어오는 배율이고, 최대 줌인은 씬의 `maximum_zoom`을 사용한다.
- 표적 중심 모드는 선택 무기의 유효·최대 사거리 조준선과 관측된 적 속도에 따른 리드 지점을 표시한다. `4 ALL WEAPONS`에서는 장착된 선택 무기 중 최대 사거리가 가장 긴 무기를 조준선 기준으로 사용한다.
- 전장 뷰포트 좌상단의 시스템 메시지는 공격 성공, 플레이어 피격·파츠 파괴, 적 파괴와 아군·플레이어 전투 불능만 최근 4건까지 표시하고 각 메시지를 일정 시간 뒤 페이드아웃한다.
- 전장 우측 표적 와이어프레임 아래에는 주요 적 로스터를 표시한다. 전투 유닛은 `DRONE`, `MECH`, `BOSS` 클래스로 구분하며 로스터에는 `MECH`와 `BOSS`만 포함하고 시나리오의 일반 드론은 제외한다.
- 시나리오 다이얼로그는 전장만 반투명 암전하고 해당 전투 노드의 처리를 정지한다. 좌클릭, `Enter`, `Space`로 다음 대사를 표시하며 종료 시 전투 노드의 기존 처리 모드를 복원한다.
- 시나리오별 대사는 `data/scenarios/`의 별도 JSON으로 관리한다. 루트는 `schema_version`, `id`, `dialogue`를 가지며 `dialogue`의 각 항목은 비어 있지 않은 `speaker`와 `text` 문자열을 가져야 한다.
- 씬 전환은 전역 `SceneTransition`을 사용한다. 위·아래 철문이 완전히 닫힌 뒤 다음 씬을 로드하고, 씬 교체가 끝나면 새 화면 위에서 철문이 열린다. 행어의 `CONFIRM LOADOUT` 전투 진입이 기본 적용 경로다.
- 엔드리스는 머리·다리·팔 파츠를 단일 스프라이트로 사용하는 경량 드론을 시간 단계에 따라 순서대로 투입한다. 모든 드론은 원본 파츠 방어력의 10%를 HP로 사용하고 기체용 방향·무기 회전 한계 디버그 선은 표시하지 않는다. 머리와 다리 드론은 접촉 시 자폭하고 팔 드론은 파츠의 무기와 총구 앵커로 원거리 공격한다.
- 엔드리스 메카는 60초에 처음 등장하고 이후 60초마다 목표 동시 메카 수가 한 기씩 증가하되 성능 보호를 위해 최대 8기로 제한한다. 메카 한 기를 격파할 때마다 플레이어의 파괴되지 않은 모든 파츠를 최대 내구도의 20%만큼 즉시 수리하며 내구도 0 또는 `disabled` 상태인 파츠·무장은 복구하지 않는다.
- 엔드리스 동시 드론 수는 최대 24기로 제한한다. 격파 점수는 머리 드론 10점, 다리 드론 20점, 팔 드론 30점, 메카 250점이며 생존 시간과 콤보 점수는 첫 구현에 포함하지 않는다.

감지와 행동 갱신 규칙:

- 센서는 실제 전장 상태를 매 프레임 직접 노출하지 않고 헤드별 감지 주기에 적 기체와 투사체 관측 스냅샷을 갱신한다. HUD, AI와 유도 미사일은 같은 최신 스냅샷을 사용한다.
- `FALCON SENSOR`는 `0.5초`마다 적 기체 2기와 투사체 4개를, `RAVEN SENSOR`는 `0.25초`마다 적 기체 4기와 투사체 8개를, `BASTION ARRAY`는 `0.125초`마다 적 기체 8기와 투사체 16개를 추적한다. 각 헤드의 감지 거리는 각각 `2200`, `2500`, `5000`이다.
- HEAD 성능은 피격 실루엣과 맞교환한다. 저성능 `FALCON SENSOR`의 탑뷰와 와이어프레임은 표준 HEAD의 1/2 크기, `RAVEN SENSOR`는 표준 크기, 고성능 `BASTION ARRAY`의 탑뷰는 표준의 2배 크기를 사용한다. Bastion 와이어프레임은 가로만 표준의 2배이고 높이는 표준과 같아 넓적한 실루엣으로 표시한다. 실제 HEAD 피격 박스는 탑뷰의 불투명 픽셀 경계에 맞춰 커진다.
- `BASTION ARRAY`의 감지 거리는 현재 최장거리 무기의 최대 사거리인 `5000`과 같게 설정해 장거리 무기의 유효 표적 정보를 끝까지 제공한다.
- 투사체 추적 한도를 넘으면 접근 중인 투사체를 우선하고 예상 충돌 시간이 짧음, 예상 피해가 큼, 거리가 가까움 순으로 선택한다. 추적되지 않은 투사체도 정상적으로 이동하고 충돌한다.
- 일반 AI 판단 주기는 `AGGRESSIVE 0.25초`, `BALANCED 0.5초`, `DEFENSIVE 1.0초`다. AI는 판단 시점에 최신 센서 스냅샷으로 이동·조준·사격 명령을 만들고 다음 판단까지 유지한다.
- 자신을 향하는 미사일 회피는 일반 AI 판단 주기를 기다리지 않고 헤드 센서 스냅샷이 갱신되는 시점에만 재평가한다. 회피 방향과 대시 결정은 다음 센서 갱신까지 유지한다.
- 플레이어의 이동, 마우스 조준과 직사 무기 발사는 센서 또는 AI 주기로 지연하지 않는다. 센서는 적 표식, 전술 지도, 경고, 유도 미사일 잠금과 유도용 관측 정보에만 적용한다.
- 전투 시작 카메라는 플레이어의 현재 유효 센서 감지 거리 전체를 보여 주는 최대 범위 줌으로 고정한다. 생존한 전투 유닛 전원이 이 화면 안에 들어오고 실제 점유 범위에 더 좁은 줌을 적용할 수 있게 된 최초 시점부터 동적 줌인을 시작한다. 이후 카메라 위치와 줌은 센서 스냅샷 주기를 따르지 않고 실제 위치를 연속적으로 보간하며, 적 표식과 전술 정보만 센서 갱신 시점에 바뀐다. 헤드 파괴 시에는 플레이어 중심의 최소 전투 시야를 유지한다.
- 동적 카메라에서 아군과 적은 센서 범위에 들어왔다는 이유만으로 프레이밍 대상에 추가하지 않는다. 현재 화면 안에 실제로 들어온 뒤에만 프레이밍 대상으로 유지하고, 센서 범위를 벗어나면 이 상태를 해제해 재진입 시 갑작스러운 줌아웃이 발생하지 않게 한다.
- 동적 카메라의 중앙점과 줌은 즉시 바꾸지 않고 프레임 독립적인 지수 보간을 사용한다. 줌아웃은 줌인보다 완만하게 전환해 조준 중 화면 구도가 갑자기 변하지 않게 한다.
- 플레이어와 AI의 유도 미사일은 현재 추적 중인 적만 새로 잠글 수 있다. 발사 후에는 마지막으로 관측한 위치와 이동 벡터를 사용하고 발사 주체의 센서 스냅샷이 갱신될 때만 유도 정보를 갱신한다.
- 한 번에 여러 발을 발사하는 미사일은 smoke trail 생성 간격을 유지하되 각 샘플의 수명과 발사 종료 후 잔존 시간을 단발 미사일의 1/5로 사용해 살아 있는 trail 샘플 수와 표시 길이를 줄인다.
- 헤드가 파괴되면 새 센서 스냅샷과 미사일 잠금을 생성하지 않는다. 이미 발사된 미사일은 마지막 관측 정보로 비행한다.

플레이어 정체 반전:

- 플레이어는 자신을 AI 지원기와 함께 싸우는 인간 조종사라고 인식한다.
- 아군은 단순 전투 성향 프리셋을 사용하는 공개된 자동 전투 지원기다.
- 스테이지 3에서 플레이어와 아군의 코어가 같은 계열임이 드러나고, 플레이어에게만 `HUMAN RESPONSE REPRODUCTION` 학습 계층이 추가됐다는 사실을 공개한다.
- 플레이어의 직접 조작과 캠페인 선택은 서사적으로 플레이어 AI가 스스로 내린 판단과 인간 행동 모사 데이터다.
- 스테이지 4부터는 기존 흐름대로 정체를 알게 된 플레이어가 인간 병사들과 작전을 수행한다.

## 기술 방향

- 내부 해상도는 480×270을 기준으로 한다.
- 픽셀 그래픽은 정수 좌표와 최근접 필터를 사용한다.
- 기체는 머리, 몸통, 양팔 장비, 백팩, 다리를 독립적으로 그려 조합 결과가 보이게 한다.
- 머리·몸통·다리는 12×12px, 긴 무장과 실루엣이 필요한 양팔 장비·백팩은 긴 변 24px와 짧은 변 12px 이내를 원칙으로 한다.
- 각 파츠는 전투용 탑뷰 이미지와 별도로 정면 와이어프레임 이미지와 앵커 맵을 가질 수 있다. `MechPartSpec.wireframe_art_path`와 `wireframe_anchor_path`는 탑뷰의 `art_path`, `anchor_path`와 분리하며, 두 뷰는 같은 앵커 이름과 조립 규칙을 사용하되 좌표는 각 뷰의 앵커 맵에서 독립적으로 읽는다.
- 정면 와이어프레임 원본은 투명 배경과 흰색 또는 밝은 회색 선으로 제작해 정상·피격·기능 정지 상태를 런타임 색상 변조로 표현한다. 정면 와이어프레임은 단순 상태 표기용이므로 파츠 조립 외의 보행·조준·발사 애니메이션을 만들지 않는다.
- 정면 조립에서는 몸통의 머리·좌우 팔·백팩·다리 소켓과 각 파츠의 `mount`를 재사용한다. 탑뷰 팔 회전에 필요한 `aim_pivot`은 정면 와이어프레임에서 요구하지 않는다.
- 기체는 보행하지 않고 지상 부스트로 미끄러지듯 이동하며, 다리 파츠 자체에는 보행 애니메이션을 만들지 않는다.
- 각 다리 파츠는 로컬 부스트 앵커를 제공하고, 이동 애니메이션은 앵커에 재사용 가능한 점화·잔광 스프라이트를 붙여 표현한다.
- 외부 이미지 에셋 대신 GDScript로 픽셀 패턴과 제한된 팔레트를 생성한다.
- 전장, 투사체, 피격 및 폭발 효과도 가능한 한 코드로 생성한다.
- 무기 피격 연출은 계열별로 구분한다. 실탄은 검정색·노란색의 작은 삼각형 파편, 에너지 무기는 흰색·파란색의 작은 삼각형 파편을 사용한다. 로켓은 별도 연기 스프라이트가 필요하지 않은 한 반투명 무채색 원을 겹친 절차적 연기로 표현한다.
- 실탄 무기는 발사 시 필수 `casing_eject` 로컬 앵커에서 작은 탄피를 하나 배출한다. 탄피는 실제 충돌 물리를 사용하지 않고 총구 정면 `±30°` 방향으로 감속 이동하며 정확히 1초 후 사라지고 회전 애니메이션은 사용하지 않는다.
- 탄피 크기는 투사체의 `visual_scale`에 비례한다. 산탄 무기도 방아쇠 입력당 셸 하나만 배출하며, 셸 길이는 유지하고 두께에만 `projectiles_per_shot / 3.0` 배율을 적용한다.
- 탄피 그림자는 처음에 탄피와 떨어진 지면 위치에서 시작해 수명 동안 탄피 위치로 수렴함으로써 공중에서 지면으로 떨어지는 느낌을 표현한다. 탄피의 무작위 배출은 전투 판정 RNG와 분리하고, 에너지 및 미사일 무기는 탄피를 생성하지 않는다.
- 에너지 무기는 발사 시 필수 `casing_eject` 앵커에서 짧은 반투명 vapor를 배출한다. vapor는 총구 정면 `±30°` 범위로 천천히 감속 이동하면서 퍼지고 `0.75초` 후 사라지며, 미사일은 이 효과를 사용하지 않는다.
- 각 전투 유닛 아래에는 배경보다 앞이고 파츠보다 뒤인 반투명 검은색 타원 그림자를 표시한다. 그림자는 기체 중심보다 약간 아래에 두며 복잡한 광원·충돌 계산 없이 크기와 투명도만 사용한다.
- 투사체 트레일도 계열별로 구분한다. 실탄은 반투명 노란색 외곽과 흰색 코어, 에너지 무기는 반투명 하늘색 외곽과 흰색 코어의 짧은 궤적을 남긴다. 로켓은 탄체 바로 뒤에 노란색 원형 화염을 표시하고 그 뒤로 흰색 원형 연기가 퍼지며 사라지게 한다.
- 유도 로켓 테스트는 적의 현재 좌표를 매 물리 프레임 추적하되 제한된 회전 속도로 방향을 보정해 곡선 궤적을 만든다. 테스트 로켓은 `400 px/s`로 이동하며 최대 5초간 생존하고, 흰색 연기 트레일은 비행 경로 전체를 길게 남긴다.
- 장거리 테스트의 기본 교전 거리는 `2000px`이다. 테스트 실탄은 사거리 `2000px`, 탄속 `2000 px/s`, 에너지탄은 사거리 `2000px`, 탄속 `4000 px/s`를 사용한다.
- 현재 장거리 AI 테스트 아레나는 `10000×10000px`이며 두 기체의 시작 거리는 `2000px`로 유지한다.
- 기체 몸통은 선택한 이동 방향으로 회전하며 일반 이동과 대시는 몸통 정면으로만 추진한다. 머리와 양팔은 장착 위치를 유지한 채 몸통 정면 기준 제한된 각도 안에서만 표적을 조준하고, 조준 한계 밖에서는 직사 무기 발사를 보류한다.
- 현재 장거리 전략 테스트에서는 기동형 AI의 몸통 회전 속도를 `67.5°/s`, 화력형 AI를 `33.75°/s`로 둔다. 무기별 조준·발사 허용각은 `WeaponSpec.traverse_limit_degrees`로 독립 관리하며 현재 테스트 무기는 모두 몸통 정면 기준 `±30°`를 사용한다.
- 무기의 조준·발사 허용각과 투사체의 최초 발사 방향은 별도 값으로 관리한다. `WeaponSpec.launch_offset_degrees`는 논리 조준 방향에 더하는 발사 방향 오프셋이며 직사 무기는 기본 `0°`, 측면 발사가 필요한 유도 미사일은 양수 또는 음수 오프셋을 사용한다. 발사 허용 여부는 오프셋을 적용하기 전 논리 조준 방향으로 판정하고, 유도 투사체는 오프셋 방향으로 생성된 뒤 자체 유도 경로를 따른다.
- 무장형 백팩은 앵커 마스크의 파란색 `muzzle` 좌표를 발사점으로 우선 사용한다. 여러 좌표가 있으면 발사마다 순서대로 사용하며, `muzzle` 앵커가 없는 기존 백팩만 백팩 루트 기준 `(0, -6)` 고정 발사점을 fallback으로 사용한다.
- 강한 무기는 발사 전 준비 시간을 요구한다. `WeaponSpec`에 준비 시간, 준비 중 이동 속도 배율과 몸통 회전 속도 배율을 두며 준비 중에는 대시할 수 없다. 탄약과 쿨다운은 실제 발사 시점에만 소비하고, 준비 완료 시 표적이 조준 한계를 벗어났다면 공격을 취소한다.
- 유도 미사일은 표적의 현재 속도로 미래 위치를 예측하고, 전투 시드로 정한 좌측·우측·후방 웨이포인트를 거쳐 종말 유도함으로써 측후면 공격 경로를 만든다.
- 조준하려는 파츠가 몸통 기준 무기 회전 한계 밖에 있으면 머리와 무기는 한계각에 걸친 채 머물지 않고 몸통 정면으로 복귀하며, 몸통이 다시 유효 조준각을 만들 때까지 발사를 보류한다.
- 팔 스프라이트의 회전은 논리 조준과 분리된 시각 효과이며 기본적으로 `45°/s`로 표적 방향을 따라간다. 논리 조준과 발사 조건이 충족되면 팔 그래픽이 아직 해당 각도에 도달하지 않았더라도 무기는 발사할 수 있다.
- 기동형 AI는 자신의 무기 사거리 안으로 접근하되 상대가 공격 준비에 들어가면 회피를 우선한다. 화력형 AI는 장거리 교전 거리를 유지하고 공격 기회를 우선하며, 적이 별도 근접 임계 거리 안에 들어왔을 때만 이탈한다.
- 발사 준비 시간은 무기 사거리 배율과 함께 조정할 수 있다. 테스트 기동형 AI는 무기 사거리와 준비 시간을 모두 기본값의 절반으로 사용한다.
- 팔 장비 설명에 `장거리` 또는 `초장거리`를 사용하는 직사 무장은 최대 사거리를 `2500~3500px` 범위로 설정한다. 산탄 계열은 중량급이라도 장거리 명칭을 사용하지 않고 산탄 확산과 근·중거리 역할을 우선한다.
- 팔 장비 설명에 `중거리`를 사용하는 무장은 최대 사거리를 `1000~1500px` 범위로 설정한다. `중형`처럼 장비 중량이나 등급을 뜻하는 표현은 거리 분류로 취급하지 않는다.
- 준비 시간이 있는 무기는 현재 표적 속도와 준비 중 가능한 이동·대시 변위를 이용해 준비 완료 시점의 사격각을 예측한다. 완료 시점에도 안전 여유각을 포함한 조준 범위 안에 표적이 남는다고 판단될 때만 공격 준비를 시작한다.
- 탄창과 재장전은 무기별로 독립 관리한다. `WeaponSpec.magazine_capacity`는 한 탄창의 발사 가능 횟수, `WeaponSpec.reload_duration`은 빈 탄창을 다시 채우는 공격 불가 시간을 정의한다. 테스트 기동형 AI의 로켓은 1발마다 기존 발사 간격만큼 재장전하며, 화력형 AI의 실탄 무기는 5발 탄창과 10초 재장전을 사용한다. 화력형 AI는 실탄 재장전 중 공격을 중단하고 적극적으로 거리를 벌리며, 재장전 완료 후에는 회피보다 공격 기회를 우선한다.
- 전투 HUD는 잔탄을 숫자로 표시하지 않고 무기별 실제 잔탄 수만큼 작은 탄약 아이콘을 그린다. 발사 시 마지막 아이콘이 즉시 탄약 열에서 빠지고 별도의 UI 탄피가 되어 아래로 낙하·회전·페이드하며, 재장전 완료 시 아이콘을 빠르게 순차 충전한다.
- HUD 탄약 아이콘은 탄마다 `Control` 노드를 만들지 않고 무기 슬롯별 단일 그리기 루틴에서 일괄 렌더링한다. 낙하하는 UI 탄피만 수명이 짧은 개별 효과로 생성하고 풀 또는 최대 개수 제한을 사용한다.
- 파츠 피격 판정은 스프라이트의 불투명 픽셀 전체를 감싸는 최소 경계 사각형을 사용한다. 바깥쪽 투명 여백은 제외하지만 경계 사각형 내부의 투명한 구멍은 피격 영역에 포함하며, 기본 전투에서는 픽셀 단위 알파 충돌을 사용하지 않는다.
- 기체 대 기체 이동 충돌은 각 기체 중심의 반지름 `28px` 단순 원으로 판정한다. 겹친 기체는 침투 거리의 절반씩 밀어내고 서로를 향한 속도 성분만 제거하며, 격파된 기체와 투사체 탄착·파츠 피격 판정에는 적용하지 않는다.
- 전장 배경이 `z_index = 0`을 사용하므로 화면에 보여야 하는 기체 파츠와 이펙트는 반드시 양수 `z_index`를 사용한다. 음수 레이어는 배경 뒤에 가려질 수 있으며, 앞뒤 순서는 양수 범위 안에서 단계별로 배정한다.
- 게임 UI는 영어를 기본으로 하되, 데이터에 포함된 한글도 웹에서 표시할 수 있어야 한다. 영문은 IBM Plex Sans Condensed Medium, 한글 fallback은 IBM Plex Sans KR Medium을 사용하고 OFL 라이선스를 배포물에 포함한다.
- 게임 데이터는 코드와 분리하고 JSON 또는 Godot Resource로 관리한다.
- 전투 로직과 화면 표현을 가능한 한 분리해 밸런스 시뮬레이션에 재사용한다.
- 같은 빌드와 같은 시드는 같은 결과를 내도록 무작위성을 통제한다.
- 기본 HUD 테스트에서 아군은 플레이어의 실제 파츠 ID 조합을 복제한다. 적의 무작위 로드아웃은 플레이어 및 다른 적과 완전히 같은 파츠 ID 조합을 중복 선택하지 않는다.
- 플레이어 무기는 테스트 AI용 사거리·연사 배율을 적용하지 않고 `WeaponSpec` 원본 수치를 사용한다. 커서 거리, 표적 조준선, AI 발사 판정과 투사체 소멸 거리는 모두 실제 다음 발사 총구를 원점으로 같은 런타임 사거리 함수를 사용한다.
- 웹 환경에서 안정적으로 동작하도록 스레드와 외부 네트워크 의존성을 사용하지 않는다.

### 메카 로드아웃과 전투 상태

- 메카의 출격 구성은 Godot `Resource`인 `MechLoadout`으로 정의한다.
- 플레이어가 현재 선택한 구성은 `player_mech_loadout`으로 관리한다. 클래스명과 인스턴스명을 혼동하는 `player_MechLoadout` 표기는 사용하지 않는다.
- `MechLoadout`은 머리, 몸통, 좌완 장비, 우완 장비, 백팩, 다리의 여섯 슬롯을 포함한다. 플레이어용 AI 카드나 AI 칩 슬롯은 포함하지 않는다.
- 로드아웃의 장착 위치는 `HEAD`, `BODY`, `LEFT_ARM`, `RIGHT_ARM`, `BACKPACK`, `LEGS`의 여섯 `MechSlot`으로 구분한다.
- 파츠 데이터의 종류는 `HEAD`, `BODY`, `ARM_EQUIPMENT`, `BACKPACK`, `LEGS`의 `PartType`으로 구분한다. 좌완과 우완은 파츠 종류가 아니라 같은 팔 장비를 장착할 수 있는 서로 다른 슬롯이다.
- 좌완과 우완은 각각 팔 구조와 무기 또는 방패가 결합된 하나의 독립 파츠로 취급한다. 장착 슬롯에 따라 발사 위치, 독립 탄약·재장전·내구도와 방패 방향을 결정한다.
- 모든 여섯 슬롯은 공통 파츠 데이터인 `MechPartSpec`을 사용하고, 팔 장비 파츠는 추가로 `WeaponSpec` 또는 `ShieldSpec`을 참조한다.
- `MechPartSpec`은 탑뷰 전투용 이미지·앵커 경로와 정면 와이어프레임용 이미지·앵커 경로를 별도로 제공한다. 뷰 차이는 `MechLoadout`에 저장하지 않고 표시 컴포넌트가 필요한 경로를 선택한다.
- 파츠와 장비 `Spec`은 변경되지 않는 설계 데이터로 공유한다. 격납고는 사전 제작된 `MechLoadout` 원본을 직접 변경하지 않고 작업용 복제본을 편집한다.
- 중량, 발전량, 전력 소비처럼 파츠에서 계산할 수 있는 합계는 로드아웃에 중복 저장하지 않는다. 출격 검증 시 현재 장착된 파츠와 모듈로 다시 계산한다.
- 메카형 적은 플레이어와 같은 `MechLoadout`과 메카 생성 루틴을 사용한다. 드론, 차량, UFO처럼 조립형 메카가 아닌 적은 유닛별 정의 데이터를 사용한다.
- 아군 AI는 플레이어의 확정된 `MechLoadout`을 복제하고 별도의 행동 성향 데이터만 추가한다.
- 전투 런타임은 `DRONE`, `MECH`, `BOSS` 클래스를 구분한다. 메카형 보스는 일반 메카와 같은 `MechLoadout` 생성 루틴을 사용한다.
- `MechLoadout`에는 현재 내구도, 탄약, EN, 열, 재장전, 쿨다운, 위치와 같은 전투 중 상태를 저장하지 않는다.
- 전투 시작 시 `MechLoadout`으로 메카 런타임을 생성하고, HUD는 로드아웃이 아니라 실제 전투 런타임 상태를 읽는다.
- 현재 프로토타입에서는 몸통을 핵심 파츠로 취급해 몸통 내구도가 0이 되면 남은 무장 상태와 관계없이 즉시 격파한다. 모든 장착 무기가 파괴된 경우도 격파로 처리하며, 한 팀의 모든 기체가 격파되면 즉시 상대 팀 승리를 확정한다. 양 팀이 같은 판정 프레임에 전멸하면 `DRAW`로 종료한다.
- 모든 전투 유닛은 HUD와 전투 시스템이 읽을 수 있는 공통 런타임 상태 인터페이스를 제공한다. 메카는 부위별 상태를 추가로 제공하고 일반 유닛은 자신에게 해당하는 상태만 제공한다.
- 전투 런타임은 전달받은 원본 `MechLoadout`이나 파츠·장비 `Spec`을 변경하지 않는다.

### 격납고와 전투 HUD 병렬 구현

이번 구현 목표는 격납고에서 플레이어 로드아웃을 설정하고 해당 로드아웃으로 전투 테스트 씬을 실행하는 것이다.

- 격납고는 실제 `MechLoadout`을 편집하고 유효성을 검사한 뒤 `player_mech_loadout`으로 확정하는 동작까지 먼저 구현한다.
- 스토리 격납고에서 선택 가능한 파츠는 출격 대상 스테이지에 따라 제한될 수 있다. 제한이 적용된 스테이지에서는 허용 목록 밖의 파츠를 후보 목록에 표시하지 않으며, 격납고 진입과 출격 확정 시 현재 로드아웃을 모두 정책에 맞게 검증한다.
- 스테이지 2 격납고는 `BD-01`, `HD-01`, `LG-03`, 양팔의 `AR-11`·`AR-15`·`AR-22`·빈 슬롯, 백팩의 `BP-05`·`BP-09`·빈 슬롯만 허용한다. 저장된 로드아웃이 이 제한을 벗어나면 `BD-01 / HD-01 / LG-03 / AR-11 / AR-11 / BP-05` 구성으로 강제 대체한다.
- 격납고의 기본 조립 프리뷰는 현재 작업 중인 `MechLoadout`을 정면으로 조립한 `MechWireframePreview`다. 이 컴포넌트는 전투 HUD의 부위 상태 도식과 공유하며, 격납고에서는 선택 파츠를 즉시 반영하고 전투 HUD에서는 런타임 피격·기능 정지 상태를 색상으로 반영한다.
- 기존 탑뷰 `MechPreview`는 격납고 기본 프리뷰로 사용하지 않고 무기 파츠 선택 중에만 발사 성능을 보여 주는 `WeaponTestPreview`에서 재사용한다.
- 팔 무기 또는 무장형 백팩 후보를 선택하거나 강조하면 현재 작업 로드아웃에 해당 후보를 임시 장착한 탑뷰 시험 장면을 표시한다. 시험 장면은 고정 표적을 향해 준비, 조준, 발사, 반동, 투사체, 탄피 배출과 재장전을 반복한다.
- 머리, 몸통, 다리와 비무장 백팩 선택에서는 탑뷰 발사 프리뷰를 실행하지 않는다.
- `WeaponTestPreview`는 원본 `MechLoadout`, `WeaponSpec`, `GameSession`과 실제 전투 탄약을 변경하지 않는 독립 프리뷰 런타임을 사용하며, 선택 UI가 닫히면 투사체와 탄피를 포함한 임시 상태를 정리한다.
- 로컬 저장은 마지막 격납고 로드아웃, 스토리 진행도와 엔드리스 최고 점수만 다룬다. 런타임 `.tres`를 덮어쓰지 않고 `user://`의 버전이 지정된 JSON을 사용하며 다중 슬롯과 클라우드 동기화는 구현하지 않는다.
- 전투 테스트 씬은 사전 제작된 `MechLoadout` 리소스로 플레이어 메카를 생성해 `GameHUD` 구현을 동시에 검증한다.
- 사전 제작 로드아웃과 격납고 로드아웃은 동일한 `MechLoadout` 타입과 동일한 메카 생성 루틴을 사용한다. HUD 테스트만을 위한 별도 임시 로드아웃 구조를 만들지 않는다.
- `GameHUD`는 전투 테스트 씬 위에 독립된 UI 씬으로 배치하고 생성된 전투 유닛의 실제 런타임 상태를 표시한다.
- `GameHUD`의 왼쪽 세로 계기판은 EN과 HEAT 게이지로 분할하고, 네 개 무장 슬롯은 숫자 잔탄 대신 탄약 아이콘과 준비·재장전 상태를 표시한다. 하단의 정면 와이어프레임은 격납고와 같은 조립 컴포넌트를 사용한다.
- 전투 커서의 거리 정보는 불투명한 면 배경을 사용하지 않고 어두운 글자 외곽선 또는 낮은 알파 배경으로 표현해 전장을 가리지 않는다.
- 전투 커서의 사거리 판정은 현재 선택한 무기 슬롯을 우선한다. `4`번 전체 무기 선택에서는 장착된 무기 중 최대 사거리가 가장 긴 무기를 기준으로 표시한다.
- 현재 센서 스냅샷에서 선택된 적의 조립 와이어프레임과 부위 상태를 검은 배경의 우측 상단 타깃 패널에 표시한다. 감지되지 않았거나 격파된 표적은 패널에서 제거한다.
- 우측 상단 타깃 와이어프레임은 패널 안에서 전장을 과도하게 가리지 않는 축소 크기로 표시하고, 검은 패널 배경은 알파 `0.5`를 사용한다.
- 전투 승리, 패배 또는 무승부가 확정되면 결과 배너를 잠시 표시한 뒤 격납고 씬으로 복귀한다.
- 기존 개발용 디버그 `Label`은 `GameHUD`와 별개로 유지하며 실제 게임 UI 구성에 포함하지 않는다.
- 플레이어 기체가 격파되어 카메라가 생존 아군을 추적하면 플레이어 전용 사이드바, 적 타깃 마커, 커서 무기·잔탄 표시와 미사일 경고를 숨긴다. 승패 결과 배너는 계속 표시한다.
- 격납고와 HUD 양쪽 구현이 완료되면 격납고에서 확정한 `player_mech_loadout`을 전투 테스트 씬에 전달하는 루틴을 연결한다.
- 유효하지 않은 로드아웃은 확정하거나 전투 씬에 전달하지 않으며, 격납고 UI에 출격 불가 원인을 표시한다.
- 최종 통합 테스트에서는 격납고에서 선택한 파츠 외형, 무장과 계산 수치가 전투 메카 및 HUD에 동일하게 반영되는지 확인한다.

### 기본 전투 HUD 후속 구현

향후 실제 게임의 스커미시 모드 기반으로 사용할 기본 HUD 테스트에는 다음 항목이 아직 구현되지 않았다.

- 실탄계 및 에너지계 무기는 발사 시 총구 앵커에서 탄종 색상에 맞는 짧은 수명의 간략한 총구 화염과 `Light2D` 효과를 발생시킨다.

목표 디렉터리 구조:

```text
nan2026-prototype/
├── Agents.md
├── Developmentplan.md
├── Gamesystem.md
├── Storyline.md
├── project.godot
├── scenes/
│   ├── main.tscn
│   ├── assembly_screen.tscn
│   ├── battle_screen.tscn
│   └── result_screen.tscn
├── scripts/
│   ├── game_controller.gd
│   ├── battle/
│   ├── ai/
│   ├── ui/
│   └── procedural_art/
├── data/
│   ├── parts.json
│   ├── directives.json
│   └── opponents.json
├── tools/
│   └── balance_simulator.gd
├── submission/
│   ├── game_description.md
│   ├── ai_technical_report.md
│   ├── prompts.md
│   └── licenses.md
└── docs/
	└── Web export
```

구조는 구현 과정에서 단순화할 수 있다. 작은 프로토타입에 불필요한 추상 계층이나 범용 프레임워크를 추가하지 않는다.

## AI 활용 및 증빙

AI 사용 자체가 제출물의 핵심이므로, 결과뿐 아니라 생성과 검토 과정을 재현할 수 있게 기록한다.

반드시 기록할 항목:

- 사용한 AI 도구와 모델
- 주요 프롬프트와 지시 사항
- AI가 제안하거나 생성한 기획, 코드, 데이터, 아트 규칙
- 사람이 검토하고 수정한 내용
- 채택하지 않은 제안과 사유
- 파츠 및 적 빌드 생성 과정
- 자동 전투 시뮬레이션 결과와 밸런스 조정 과정
- 외부 도구, 오픈소스 및 에셋의 출처와 라이선스

권장 작업 흐름:

```text
AI가 파츠와 적 빌드 후보 생성
→ 구조화 데이터 검증
→ 자동 전투 시뮬레이션
→ 승률, 피해량, 전투 시간 집계
→ AI가 이상 조합 분석
→ 사람이 최종 수치 결정
```

AI가 생성한 결과를 그대로 사용했다고 과장하지 않는다. `AI 제안 → 구현 → 테스트 → 인간 검토`의 반복 과정을 사실대로 기술한다.

## 추후 작업

- 타이틀 또는 옵션 화면에 별도의 `LICENSES` 메뉴를 추가한다.
- 라이선스 화면에는 Godot 엔진과 IBM Plex를 포함한 외부 소프트웨어·폰트·에셋의 명칭, 저작권자, 출처와 라이선스 원문 또는 원문으로 이동할 수 있는 항목을 표시한다.
- 라이선스 메뉴가 구현되기 전까지는 저장소의 라이선스 문서와 웹 배포물에 포함된 동반 파일로 고지 의무를 충족한다.

## 제출물

개인 참가이므로 팀원 역할 기술서는 생략한다. 다음 네 가지를 완성해야 한다.

1. GitHub 저장소의 전체 소스와 GitHub Pages 웹 빌드
2. 실제 플레이 화면으로 구성한 30~60초 YouTube 영상
3. 게임 소개 및 실행 방법 PDF
4. AI 활용 기술 PDF

저장소에는 의미 있는 커밋 기록을 유지한다. 제출 링크는 심사 종료 시점까지 접근 가능해야 하며, 접수 마감 후 내용을 변경할 수 없다는 전제로 준비한다.

## 일정

상세 작업 순서와 단계별 완료 조건은 `Developmentplan.md`를 따른다.

| 날짜 | 목표 |
| --- | --- |
| 7월 31일 | 프로젝트 문서와 데이터 구조 확정 |
| 8월 1일 | 수동 테스트 기체, 실탄, 파츠 피격 |
| 8월 2일 | EN·열, 에너지·미사일·방패 |
| 8월 3일 | 플레이어 조작과 아군·적 행동 패턴 |
| 8월 4일 | 개발용 스커미시와 전투 UI |
| 8월 5일 | 격납고, 통신, 스테이지 실행기 및 전투 기능 동결 |
| 8월 6일 | 스테이지 1~3 |
| 8월 7일 | 스테이지 4 구조 분기와 스테이지 5 지원·레일건 |
| 8월 8일 | 압축형 최종전, 웹 배포와 브라우저 테스트 |
| 8월 9일 | 영상 촬영 및 PDF 작성 |
| 8월 10일 | 링크와 제출물 최종 검증 |

8월 5일 이후에는 새로운 전투 기능을 추가하지 않는다. 이후에는 기존 기능으로 스테이지 데이터와 연출을 구성하고 버그 수정, 가독성 개선, 제출물 검증만 수행한다.

## 완료 기준

- 새 브라우저 세션에서 링크를 열어 오류 없이 시작된다.
- 화면의 짧은 안내만 보고 `WASD`와 마우스로 이동·조준·사격할 수 있다.
- 개별 전투는 5분 이내, 전체 캠페인은 30분 이내에 완료할 수 있다.
- 다섯 스테이지와 엔딩을 중단 없이 완료할 수 있다.
- 파츠 조합이 외형과 전투 수치에 모두 반영된다.
- 선택한 아군 AI 성향이 이동, 표적 선택과 생존 방식에 관찰 가능한 차이를 만든다.
- 아군·적의 행동 상태와 전투 후 분석을 통해 패배 원인을 이해할 수 있다.
- 스테이지 1·2의 2대2 전투와 스테이지 3의 2대1 보스전을 중단 없이 완료할 수 있다.
- 외부 네트워크 요청 없이 전체 게임이 실행된다.
- Chromium 및 Firefox 계열 브라우저에서 웹 빌드를 확인한다.
- 모든 외부 소스와 라이선스가 문서에 기록된다.
- GitHub, YouTube, PDF의 링크와 접근 권한을 최종 확인한다.
